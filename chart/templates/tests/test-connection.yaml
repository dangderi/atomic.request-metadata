apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atomic-request-metadata.fullname" . }}-test-connection"
  labels:
    {{- include "atomic-request-metadata.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atomic-request-metadata.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
