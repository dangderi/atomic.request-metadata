apiVersion: v1
kind: Service
metadata:
  name: {{ include "atomic-request-metadata.fullname" . }}
  labels:
    {{- include "atomic-request-metadata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atomic-request-metadata.selectorLabels" . | nindent 4 }}
